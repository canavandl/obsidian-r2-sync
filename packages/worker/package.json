{
  "name": "@obsidian-r2-sync/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "clean": "rm -rf dist .turbo .wrangler",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@obsidian-r2-sync/shared": "workspace:*",
    "hono": "^4.7.0",
    "aws4fetch": "^1.0.20"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.99.0"
  }
}
